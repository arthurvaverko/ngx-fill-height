import { FillHeightDirective } from './fill-height.directive';

describe('FillHeightDirective', () => {
	it('should create an instance', () => {
		const directive = new FillHeightDirective();
		expect(directive).toBeTruthy();
	});
});
